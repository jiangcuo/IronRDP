# ironrdp-rdpei

Remote Desktop Protocol Input Virtual Channel Extension (`Microsoft::Windows::RDS::Input`).

This crate implements the client-side RDPEI handshake and touch event encoding used for native
multi-touch injection.
